---
tags: [{{PROJECT_NAME}}, log]
date: {{DATE}}
author: {{AUTHOR}}
---

# {{PROJECT_NAME}} — Append-Only Log

> [!warning] Append-only
> Add new entries at the **bottom**. Never edit or reorder past entries. Format: `## [YYYY-MM-DD] <type> | <title>` where `<type>` ∈ {ingest, update, lint, refactor, decision}.

---

## [{{DATE}}] decision | Methodology adopted
- Adopted the three-layer wiki pattern with pointer-architecture schema.
- Created `index.md`, this `log.md`, `concepts/`, `entities/`.
- Skills installed: `/ingest`, `/lint-wiki`, `/token-checkup`.
- Dashboard available via `{{COMMAND_PREFIX}} dashboard`.
- **Result:** Token feedback loop active, baseline pending first measurement.
